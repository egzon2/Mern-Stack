//To run the project, follow these steps

1. npm install -global yarn

2. yarn install

3. yarn add react-scripts

4. Go to backend directory -> run this command -> node server.js

5. Go to frontend directory -> run this command -> yarn start